selector = new SelectionBox();

selector_sprite_w = sprite_get_width(spr_selector_default);
selector_sprite_h = sprite_get_height(spr_selector_default);

// InputType: joystick=0, keyboard=1, gamepad=2, ai=3

// No Create do objeto que desenha a selection box:

